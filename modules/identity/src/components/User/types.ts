export interface UserInGroup {
	id: string;
	email: string;
	displayName?: string;
	avatarUrl?: string;
	status?: string;
}

export interface AvailableUser {
	id: string;
	email: string;
	displayName?: string;
	avatarUrl?: string;
}