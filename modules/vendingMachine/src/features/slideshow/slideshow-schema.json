{
	"name": "slideshow",
	"fields": {
		"id": {
			"type": "string",
			"label": "{ \"$ref\": 'nikki.vendingMachine.slideshow.fields.id' }",
			"hidden": true
		},
		"code": {
			"type": "string",
			"label": "{ \"$ref\": 'nikki.vendingMachine.slideshow.fields.code' }"
		},
		"name": {
			"type": "string",
			"label": "{ \"$ref\": 'nikki.vendingMachine.slideshow.fields.name' }"
		},
		"description": {
			"type": "string",
			"label": "{ \"$ref\": 'nikki.vendingMachine.slideshow.fields.description' }"
		},
		"status": {
			"type": "enum",
			"label": "{ \"$ref\": 'nikki.vendingMachine.slideshow.fields.status' }",
			"enum": [
				{
					"value": "active",
					"label": "{ \"$ref\": 'nikki.general.status.active' }"
				},
				{
					"value": "inactive",
					"label": "{ \"$ref\": 'nikki.general.status.inactive' }"
				},
				{
					"value": "expired",
					"label": "{ \"$ref\": 'nikki.vendingMachine.slideshow.status.expired' }"
				}
			]
		},
		"startDate": {
			"type": "date",
			"label": "{ \"$ref\": 'nikki.vendingMachine.slideshow.fields.startDate' }"
		},
		"endDate": {
			"type": "date",
			"label": "{ \"$ref\": 'nikki.vendingMachine.slideshow.fields.endDate' }"
		},
		"createdAt": {
			"type": "date",
			"label": "{ \"$ref\": 'nikki.vendingMachine.slideshow.fields.createdAt' }",
			"hidden": true
		}
	}
}
