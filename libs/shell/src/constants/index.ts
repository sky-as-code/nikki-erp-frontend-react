
export const MODULE_SCHEMA_NAME = 'essential_module_metadata';

/**
 * The org home page — the module list.
 *
 * The organization is no longer a URL segment, so org home is the app root. Switching org
 * reloads onto this, since the page the user was on may name a record the new org does not have.
 */
export const ORG_HOME_PATH = '/';
